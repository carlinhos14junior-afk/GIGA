# 🚀 GIGATEL Fibra Óptica - Guia de Deploy e Configuração

Este é o código-fonte de um site profissional, dinâmico e moderno para o provedor de internet **GIGATEL**, construído com **React**, **Vite**, **TypeScript** e **Tailwind CSS v4**, preparado para integração com o bando de dados gratuito **Supabase** e hospedagem na **Vercel**.

---

## 🛠️ Passo 1: Configuração do Banco de Dados no Supabase

1. Crie uma conta gratuita em [Supabase.com](https://supabase.com/).
2. Inicie um novo projeto chamado **GIGATEL** e aguarde a inicialização da base de dados PostgreSQL.
3. No painel de controle esquerdo do Supabase, acesse a aba **SQL Editor** (ícone de terminal de SQL `>_`).
4. Clique em **"New query"** e cole o seguinte script para criar as tabelas necessárias e alimentar as configurações do site automaticamente:

```sql
-- 1. CRIAR TABELA DE CONFIGURAÇÕES DO SITE
CREATE TABLE IF NOT EXISTS configuracoes_site (
  id BIGINT PRIMARY KEY DEFAULT 1,
  nome_empresa VARCHAR(255) DEFAULT 'GIGATEL',
  logo_url TEXT,
  logo_white_url TEXT,
  logo_footer_url TEXT,
  logo_mobile_url TEXT,
  favicon_url TEXT,
  whatsapp VARCHAR(50) DEFAULT '5511910050121',
  telefone VARCHAR(50) DEFAULT '(11) 91005-0121',
  email VARCHAR(255) DEFAULT 'contato@gigatel.com.br',
  endereco TEXT DEFAULT 'Rua Antônio Ferraciolli, 331',
  bairro VARCHAR(100) DEFAULT 'Jardim Catarina',
  cidade VARCHAR(100) DEFAULT 'São Paulo',
  estado VARCHAR(2) DEFAULT 'SP',
  cep VARCHAR(10) DEFAULT '03910-070',
  instagram VARCHAR(100) DEFAULT 'gigatelofc',
  facebook VARCHAR(100) DEFAULT 'gigatelofc',
  site_status VARCHAR(50) DEFAULT 'Ativo'
);

-- INSERIR DADOS INICIAIS DE CONFIGURAÇÃO (OPCIONAL)
INSERT INTO configuracoes_site (id, nome_empresa, whatsapp, telefone, email, endereco, bairro, cidade, estado, cep, instagram, facebook, site_status)
VALUES (1, 'GIGATEL', '5511910050121', '(11) 91005-0121', 'contato@gigatel.com.br', 'Rua Antônio Ferraciolli, 331', 'Jardim Catarina', 'São Paulo', 'SP', '03910-070', 'gigatelofc', 'gigatelofc', 'Ativo')
ON CONFLICT (id) DO NOTHING;

-- 2. CRIAR TABELA DE PLANOS
CREATE TABLE IF NOT EXISTS planos (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  nome VARCHAR(255) NOT NULL,
  velocidade VARCHAR(100) NOT NULL,
  preco NUMERIC(10,2) NOT NULL,
  descricao TEXT,
  beneficios JSONB DEFAULT '[]'::jsonb,
  ordem INT,
  destaque BOOLEAN DEFAULT false,
  ativo BOOLEAN DEFAULT true,
  detalhes TEXT,
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- INSERIR PLANOS PADRÃO GIGATEL
INSERT INTO planos (nome, velocidade, preco, beneficios, destaque, ativo)
VALUES 
('Plano Smart 500 Mega', '500 MEGA', 99.90, '["100% Fibra Óptica", "Wi-Fi incluso", "Download ilimitado", "Suporte técnico humanizado"]'::jsonb, false, true),
('Plano Ultra 800 Mega', '800 MEGA', 129.90, '["100% Fibra Óptica", "Wi-Fi Dual Band incluso", "Download e Upload de alta performance", "Ideal para jogos e streaming", "Instalação expressa"]'::jsonb, true, true)
ON CONFLICT DO NOTHING;

-- 3. CRIAR TABELA DE SEO
CREATE TABLE IF NOT EXISTS seo (
  id BIGINT PRIMARY KEY DEFAULT 1,
  title VARCHAR(255) DEFAULT 'GIGATEL - Internet Ultraveloz',
  meta_description TEXT DEFAULT 'Internet Fibra Ultraveloz para sua casa.',
  keywords VARCHAR(255) DEFAULT 'GIGATEL, INTERNET, FIBRA',
  open_graph_title VARCHAR(255),
  open_graph_description TEXT,
  imagem_compartilhamento TEXT,
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

INSERT INTO seo (id, title, meta_description, keywords, status)
VALUES (1, 'GIGATEL - Internet Ultraveloz', 'Internet Fibra Ultraveloz para sua casa.', 'GIGATEL, INTERNET, FIBRA', 'ativo')
ON CONFLICT (id) DO NOTHING;

-- 4. CRIAR TABELA DE REDES SOCIAIS
CREATE TABLE IF NOT EXISTS redes_sociais (
  id BIGINT PRIMARY KEY DEFAULT 1,
  instagram VARCHAR(100) DEFAULT 'gigatelofc',
  facebook VARCHAR(100) DEFAULT 'gigatelofc',
  youtube VARCHAR(100),
  tiktok VARCHAR(100),
  linkedin VARCHAR(100),
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

INSERT INTO redes_sociais (id, instagram, facebook, status)
VALUES (1, 'gigatelofc', 'gigatelofc', 'ativo')
ON CONFLICT (id) DO NOTHING;

-- 5. CRIAR TABELA DE BRAND SETTINGS
CREATE TABLE IF NOT EXISTS brand_settings (
  id BIGINT PRIMARY KEY DEFAULT 1,
  logo_url TEXT,
  logo_white_url TEXT,
  logo_footer_url TEXT,
  logo_mobile_url TEXT,
  favicon_url TEXT,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

INSERT INTO brand_settings (id, logo_url, logo_white_url, logo_footer_url, logo_mobile_url, favicon_url)
VALUES (1, '', '', '', '', '')
ON CONFLICT (id) DO NOTHING;

-- 6. CRIAR TABELA DE CIDADES DE COBERTURA
CREATE TABLE IF NOT EXISTS cidades_cobertura (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  nome VARCHAR(255) NOT NULL,
  estado VARCHAR(2) NOT NULL,
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 7. CRIAR TABELA DE LEADS
CREATE TABLE IF NOT EXISTS leads (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  nome VARCHAR(255) NOT NULL,
  whatsapp VARCHAR(50) NOT NULL,
  email VARCHAR(255),
  cep VARCHAR(10),
  endereco TEXT,
  plano_interesse VARCHAR(255),
  observacoes TEXT,
  status VARCHAR(50) DEFAULT 'novo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 8. CRIAR TABELA DE EMPRESA
CREATE TABLE IF NOT EXISTS empresa (
  id BIGINT PRIMARY KEY DEFAULT 1,
  nome_empresa VARCHAR(255) DEFAULT 'GIGATEL',
  telefone VARCHAR(50) DEFAULT '(11) 91005-0121',
  whatsapp VARCHAR(50) DEFAULT '5511910050121',
  email VARCHAR(255) DEFAULT 'contato@gigatel.com.br',
  endereco TEXT DEFAULT 'Rua Antônio Ferraciolli, 331',
  numero VARCHAR(50) DEFAULT '331',
  bairro VARCHAR(150) DEFAULT 'Jardim Catarina',
  cidade VARCHAR(150) DEFAULT 'São Paulo',
  estado VARCHAR(2) DEFAULT 'SP',
  cep VARCHAR(10) DEFAULT '03910-070',
  latitude VARCHAR(100),
  longitude VARCHAR(100),
  horario_funcionamento VARCHAR(255),
  status VARCHAR(50) DEFAULT 'ativo',
  cnpj VARCHAR(50),
  instagram VARCHAR(100),
  facebook VARCHAR(100),
  logo_url TEXT,
  logo_white_url TEXT,
  logo_footer_url TEXT,
  logo_mobile_url TEXT,
  favicon_url TEXT,
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

INSERT INTO empresa (id, nome_empresa, telefone, whatsapp, email, endereco, numero, bairro, cidade, estado, cep, status)
VALUES (1, 'GIGATEL', '(11) 91005-0121', '5511910050121', 'contato@gigatel.com.br', 'Rua Antônio Ferraciolli, 331', '331', 'Jardim Catarina', 'São Paulo', 'SP', '03910-070', 'ativo')
ON CONFLICT (id) DO NOTHING;

-- 9. CRIAR TABELA DE USUARIOS
CREATE TABLE IF NOT EXISTS usuarios (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  nome VARCHAR(255) NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  perfil VARCHAR(50) DEFAULT 'admin',
  nivel VARCHAR(50),
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 10. CRIAR TABELA DE BANNERS
CREATE TABLE IF NOT EXISTS banners (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  titulo VARCHAR(255),
  subtitulo VARCHAR(255),
  texto_botao VARCHAR(255),
  link_botao TEXT,
  image_url TEXT,
  mobile_image_url TEXT,
  ordem INT DEFAULT 0,
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 11. CRIAR TABELA DE HERO SECTION
CREATE TABLE IF NOT EXISTS hero_section (
  id VARCHAR(255) PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  subtitle VARCHAR(255) NOT NULL,
  image_url TEXT,
  active BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 12. CRIAR TABELA DE BLOG POSTS
CREATE TABLE IF NOT EXISTS blog_posts (
  id VARCHAR(255) PRIMARY KEY,
  title VARCHAR(255) NOT NULL,
  slug VARCHAR(255) UNIQUE NOT NULL,
  content TEXT NOT NULL,
  image_url TEXT,
  published BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- 13. CRIAR TABELA DE UPLOAD MEDIA
CREATE TABLE IF NOT EXISTS uploads (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  nome VARCHAR(255) NOT NULL,
  url TEXT NOT NULL,
  tamanho VARCHAR(100),
  tipo VARCHAR(100),
  status VARCHAR(50) DEFAULT 'ativo',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```
5. Clique em **Run** no topo direito para executar as queries. Suas tabelas estarão prontas!

---

## 💻 Passo 2: Subir o Projeto para o GitHub

1. Baixe o código fonte do projeto (exporte em ZIP ou conecte ao Github pelo menu Settings da AI Studio).
2. Na sua máquina local, abra o terminal no diretório do projeto e inicialize o Git:
```bash
git init
```
3. Adicione todos os arquivos e faça o commit inicial:
```bash
git add .
git commit -m "feat: site GIGATEL completo com gerenciador e leads"
```
4. Crie um repositório vazio no seu **GitHub** (ex: `gigatel-fibra`).
5. Vincule o repositório local ao GitHub e faça o push:
```bash
git branch -M main
git remote add origin https://github.com/SEU_USUARIO/gigatel-fibra.git
git push -u origin main
```

---

## ⚡ Passo 3: Deploy Gratuito na Vercel

1. Acesse **[Vercel.com](https://vercel.com/)** e faça login com sua conta do GitHub.
2. Clique em **"Add New"** > **"Project"**.
3. Importe o repositório `gigatel-fibra` que você acabou de criar no GitHub.
4. Na tela de configuração do projeto na Vercel, clique e expanda a aba **Environment Variables** (Variáveis de Ambiente).
5. Escreva e adicione as seguintes variáveis com as credenciais obtidas nas configurações do seu projeto Supabase (em **Project Settings** > **API**):

   * **Chave 1**: `VITE_SUPABASE_URL`  
     * *Valor*: *(Insira a URL de API do projeto no Supabase, ex: `https://xxxx.supabase.co`)*
   
   * **Chave 2**: `VITE_SUPABASE_ANON_KEY`  
     * *Valor*: *(Insira a chave pública de acesso anônimo do Supabase)*

6. Clique no botão **"Deploy"**! A Vercel compilará as dependências e disponibilizará o site ao vivo em um link público HTTPS em menos de 1 minuto.

---

## 🗝️ Acesso ao Painel Administrativo (/admin)

Para acessar e testar o painel administrativo:
1. Deslize até o rodapé ou acesse o link do seu site e clique no botão **Área do Cliente** ou clique no link **Área Administrativa** no topo.
2. Se você já configurou as variáveis do Supabase, você poderá usar os recursos do Supabase Auth para autenticar.
3. Se você estiver testando na prévia do AI Studio (sem variáveis configuradas), o sistema de **Simulação Inteligente** salvará tudo no seu `localStorage` local automaticamente sem quebrar. É possível fazer login usando as seguintes credenciais de teste:
   * **E-mail**: `admin@gigatel.com.br`
   * **Senha**: `admin123`
